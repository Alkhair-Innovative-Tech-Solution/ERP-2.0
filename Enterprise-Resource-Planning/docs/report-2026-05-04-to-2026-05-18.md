# Bi-Weekly Project Report
**Period:** 4 May 2026 – 18 May 2026
**Project:** ERP System — Auth-Service + HDMS Integration
**Prepared:** 18 May 2026

---

## 1. Executive Summary

Last two weeks mein Auth-Service ka core CRUD layer fully stabilized ho gaya —
employee create/edit, branch/department/designation forms, aur assignment endpoints sab E2E verified hain.
VMS ↔ Auth-Service SSO integration production-ready state mein hai.
Ek infrastructure blocker aa gaya hai (server pe WiFi nahi chal raha) jiska daily action plan
is report ke Section 5 mein hai.

---

## 2. Completed Work

### 2a. Auth-Service — Backend

| # | What | Status |
|---|------|--------|
| 1 | `EmployeeCreateSchema` / `EmployeeUpdateSchema` validators added — DOB age check (min 13), gender enum, shift enum, employment type enum | ✅ Done |
| 2 | `EmployeeAssignment` dedicated endpoints (POST / PUT / DELETE) with `_assignment_response()` helper | ✅ Done |
| 3 | `joining_date` NOT NULL fix — defaults to `date.today()` when omitted | ✅ Done |
| 4 | `PUT /employees/{id}` endpoint added — all fields optional, CNIC uniqueness excludes self | ✅ Done |
| 5 | `GET /employees/{id}` accepts `employee_id` OR `employee_code` via `Q()` — frontend list was silently 404ing | ✅ Done |
| 6 | `org_email` empty-string → NULL coercion before save (unique constraint collision fix) | ✅ Done |
| 7 | `payload: dict` → `payload: Body[dict]` on create/update Institution + Branch — JSON body was routing as query param, causing 422 on every POST | ✅ Done |
| 8 | `DesignationUpdateSchema` fields changed `str = None` → `Optional[str] = None` — Pydantic v2 was rejecting null | ✅ Done |
| 9 | New endpoints: `PUT /branches/{branch_key}` + `DELETE /branches/{branch_key}` (soft-delete via `branch_code` OR `branch_id`) | ✅ Done |
| 10 | Stale `EmployeeAssignment.institution` FK reference removed from `employees/api.py` — was crashing `get_institution` + `update_institution` with `AttributeError` | ✅ Done |
| 11 | Django Admin `AssignmentInline.fields` fixed — removed non-existent `institution` field, restored `branch` | ✅ Done |
| 12 | Stale container rebuilt after model field removal (`state` column) — ProgrammingError resolved | ✅ Done |
| 13 | Employee list bugfix — invalid prefetch path `assignments__institution` replaced with `assignments__branch__institution` + `assignments__department__institution` | ✅ Done |

### 2b. Auth-Service — Frontend

| # | What | Status |
|---|------|--------|
| 14 | `employees/new/page.tsx` — full rewrite to `react-hook-form` + `zod`, 4-step form (Identity / Contact / Placement / History), cascading dropdowns, inline errors | ✅ Done |
| 15 | `employees/[id]/edit/page.tsx` — same pattern + `reset()` pre-populate from GET | ✅ Done |
| 16 | `branches/page.tsx` — modal rebuilt with rhf+zod, per-field inline errors, backend `field_errors` surfaced | ✅ Done |
| 17 | `departments/page.tsx` — same treatment, `dept_code` locked on edit | ✅ Done |
| 18 | `designations/page.tsx` — same treatment, `position_code` locked on edit | ✅ Done |
| 19 | `institutions/new/page.tsx` + `institutions/[id]/edit/page.tsx` — full rewrite, org pill-picker, `reset()` pre-populate | ✅ Done |
| 20 | `PhoneInput.tsx` (max 11 digits, auto-dash `XXXX-XXXXXXX`) + `CNICInput.tsx` (13 digits, `XXXXX-XXXXXXX-X`) — new masked components | ✅ Done |
| 21 | 5 frontend URL bugs fixed (double-prefix, missing leading slash, wrong redirect paths) | ✅ Done |
| 22 | `settings/page.tsx` deleted — UI-only page, not wired, not linked from nav | ✅ Done |
| 23 | Gender "other" option removed across all forms — backend validator rejects it | ✅ Done |

### 2c. VMS ↔ Auth-Service Integration

| # | What | Status |
|---|------|--------|
| 24 | `VmsRole` SoftDelete columns added (migration `0005_fix_vmsrole_softdelete_fields`) | ✅ Done |
| 25 | `VmsLoginView` — auth-service proxy with local JWT fallback, amber toast on 503 | ✅ Done |
| 26 | `VmsHybridAuthentication` DRF class — detects token type, Redis-cached `/me` validation | ✅ Done |
| 27 | 9 Playwright E2E scenarios verified — happy path, wrong password, 503 fallback, Redis cache | ✅ Done |
| 28 | VMS nginx port conflict fixed (moved to `8080:80`), Redis alias collision fixed (`vms-redis`) | ✅ Done |

### 2d. Infrastructure

| # | What | Status |
|---|------|--------|
| 29 | ERD diagram generated — all 17 Auth-Service models with fields + relationships (`docs/erd.md`) | ✅ Done |
| 30 | `CLAUDE.md` added to Auth-Service — dev commands, architecture map, known gotchas documented | ✅ Done |
| 31 | Intern branch `maryam` sync fixed — was tracking wrong upstream, merge commit applied | ✅ Done |

---

## 3. In Progress

| Feature | Notes |
|---------|-------|
| Next.js frontend pages | login, employees, institutions, branches, departments, designations, profile, audit-logs scaffolded — wiring in progress |
| Audit log UI | `/api/audit/*` endpoints exist; frontend page is scaffold only |
| DOB age validation — frontend | Backend rejects <13 years; zod schema on frontend not yet added |
| RS256 JWT migration | Plan written (`docs/superpowers/plans/2026-04-25-rs256-jwt-migration.md`), not yet executed |

---

## 4. Planned (Not Started)

| Feature | Priority | Notes |
|---------|----------|-------|
| Remove redundant FKs on `EmployeeAssignment` | High | Drop `branch` + `institution`, derive via `designation.department.branch.institution` — architecture report §3 |
| OpenAPI → TypeScript type generation | Medium | Sync backend schemas into `frontend/src/types/` |
| Tighten CORS / ALLOWED_HOSTS for prod | **Critical before prod** | Currently `*` on both — must gate |
| `Department` parent via Generic FK | Low (Future) | Replace 3 nullable FKs with Django Content Types |

---

## 5. Blockers

### 🔴 BLOCKER 1 — Server pe WiFi nahi chal raha

**Description:** Development server ka WiFi connection down hai — remote access, Docker container connectivity, aur deployment testing affected hain.
**Impact:** Auth-service + HDMS containers ka live testing delayed. Frontend dev server access affected.
**Owner:** Ubaid

#### Daily Action Plan (Sundays Skip)

| Date | Day | Action |
|------|-----|--------|
| **18 May** | Mon | IT department ko formal report karo — affected systems list bhejo. Wired ethernet cable se backup access check karo. |
| **19 May** | Tue | Mobile hotspot se Docker + auth-service connectivity test karo. Hotspot IP ko `next.config.ts` LAN whitelist mein add karo. |
| **20 May** | Wed | IT se ETA lo fix ka. Agar ETA > 3 din: permanent workaround (ethernet switch / USB WiFi adapter) order karo. |
| **21 May** | Thu | Workaround implement karo — ethernet cable ya USB adapter lagao, server se all services ping test karo. |
| **22 May** | Fri | Full connectivity test: Auth-service login → HDMS ticket create → VMS check-in. Document jo kaam kiya jo nahi. |
| **23 May** | Sat | RS256 JWT migration plan execute karo (blocked kaam jo WiFi pe dependent nahi) — is waqt karo. |
| **25 May** | Mon | IT follow-up #2. Permanent fix confirm karo ya escalate karo. Network config (static IP, DNS) verify karo. |
| **26 May** | Tue | Full E2E Playwright test suite chalao new network config pe. Failures log karo. |
| **27 May** | Wed | Audit log UI wiring start karo — frontend page scaffold se real API tak. |
| **28 May** | Thu | Agar WiFi abhi tak fix nahi: IT management ko escalate karo, router/AP replacement maango. |
| **29 May** | Fri | Blocker close karo agar resolved. Lesson learned update karo `PROJECT_LOG.md` mein. |
| **30 May** | Sat | CORS / ALLOWED_HOSTS prod hardening — yeh WiFi se independent hai, karo. |

---

## 6. Tech Debt Tracker

| Debt | Risk | When to Fix |
|------|------|-------------|
| `CORS_ALLOW_ALL_ORIGINS = True` + `ALLOWED_HOSTS = ['*']` | 🔴 Critical | Before ANY prod deploy |
| `EmployeeAssignment` redundant `branch` + `institution` FKs | 🟡 Medium | Next sprint |
| `Department` — 3 nullable parent FKs | 🟡 Medium | Future sprint |
| `--fake-initial` in Docker CMD | 🟢 Low | On next schema migration |
| Frontend DOB zod validation missing | 🟢 Low | This sprint |

---

## 7. E2E Test Coverage

| Area | Scenarios | Status |
|------|-----------|--------|
| Auth-Service CRUD (employees) | Create, Edit, Validation (CNIC, phone, DOB, gender) | ✅ 12/12 pass |
| Auth-Service CRUD (branches / depts / designations / institutions) | Create, Edit, Delete, Validation | ✅ 12/12 pass |
| VMS ↔ Auth-Service SSO | Login, wrong password, 503 fallback, Redis cache, token verify | ✅ 9/9 pass |
| Assignment endpoints | POST, PUT, DELETE | ✅ Pass |

---

## 8. Key Commits This Period

| Commit | Description |
|--------|-------------|
| `57111e4` | feat: validation hardening + masked inputs (PhoneInput, CNICInput) |
| `f95666f` | fix: joining_date NOT NULL violation in assignment create |
| `66198f6` | fix(permissions): SoftDelete columns added to VmsRole (migration 0005) |
| `760312f` | fix(vms): E2E integration bugs (nginx port, redis alias, auth class, axios interceptor) |
| `b5c32c9` | feat(auth): VMS service support — dynamic service registry + VmsRole |
| `14b44ac` | refactor: removed institution FK from EmployeeAssignment (P1 cleanup) |

---

*Report generated from `Auth-service/PROJECT_LOG.md` + `erp_new/PROJECT_LOG.md`*
*Next report due: 1 June 2026*
